{
  "name": "nest-test-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -c auto -n backend,frontend \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
